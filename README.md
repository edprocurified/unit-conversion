# Repo to quickly test UOM conversion.

This relies on OpenAI agents SDK with custom tool to perform calculations, asynchronously, for a confiugrable `BATCH_SIZE`

Testing:

[✅] Imperial to Metric conversion

[] Per unit conversion